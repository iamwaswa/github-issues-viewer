import { Theme } from 'src/types';

const theme: Theme = {
  colors: {
    primary: `#212121`,
    secondary: `#FAFAFA`,
    boxShadow: `rgba(0, 0, 0, 0.1)`,
    error: `red`,
  },
  fonts: ['sans-serif', 'Roboto'],
};

export default theme;
